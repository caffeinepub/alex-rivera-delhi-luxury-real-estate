export interface LocalProperty {
  id: number;
  city: string;
  title: string;
  price: number;
  photos: [string, string, string];
  enquiryCount: number;
  location: string;
  lat: number;
  lng: number;
}
